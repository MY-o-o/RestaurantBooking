[build]
buildCommand = "pip install -r requirements.txt && python manage.py collectstatic --noinput"

[deploy]
startCommand = "python manage.py migrate --noinput && (python manage.py createsuperuser --noinput || true) && daphne RestaurantBooking.asgi:application --bind 0.0.0.0 --port $PORT"
